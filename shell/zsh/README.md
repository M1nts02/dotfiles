# zsh

- `brew install zsh-autosuggestions zsh-syntax-highlighting starship eza fzf zoxide`
- link `zshrc` into `~/.zshrc`
- `mkdir ~/.zsh`
- `git clone https://github.com/Aloxaf/fzf-tab --depth 1 ~/.zsh/`
