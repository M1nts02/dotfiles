hs.hotkey.bind({ "cmd" }, "`", function()
  hs.execute("flashspace focus --next-window", true)
end)

hs.hotkey.bind({ "cmd", "shift" }, "`", function()
  hs.execute("flashspace focus --next-window", true)
end)
